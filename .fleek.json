{
    "build": {
      "image": "fleek/create-react-app:node-16",
      "command": "cd.. && yarn && cd ../hardhat-zone && yarn hardhat compile && cd ../react-zone && yarn build",
      "publicDir": "build",
      "baseDir": "react-zone",
      "environment": {
        "SOME_ENV": "Build finished!"
      }
    }
   }